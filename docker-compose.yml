services:
  api:
    build: .
    container_name: fastapi_dev
    ports:
      - "8000:8000"
    volumes:
      # Bind mount the current directory to /app in the container for hot-reloading
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
